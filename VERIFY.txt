Arabic Hunt Battle v0.7.0 verification notes

Static/type verification in build workspace:
- TypeScript: tsc --noEmit PASS
- AR mode feature flag enabled
- AR flow uses existing server-authoritative Supabase match/claim RPCs
- v0.7 SQL patch is non-destructive and adds AR leaderboard
- WebXR lifecycle/hit test/DOM overlay code targets PlayCanvas 2.22.x APIs

Not hardware-verified in this build environment:
- Immersive AR camera session
- real-device hit-test quality
- anchor availability
These require a compatible physical WebXR device over HTTPS. Run REALTIME-TEST-CHECKLIST.md before classroom deployment.
