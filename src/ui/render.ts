import { VOCAB } from '../data/vocabulary';
import { WORLD_CARDS, getWorld, getWorldCard } from '../worlds/catalog';
import type { MatchSnapshot } from '../services/realtimeTypes';
import type { AppUser } from '../services/AuthService';
import type { AdminAccount } from '../services/AdminUserService';
import type { LearningClass, MatchHistoryRow, LeaderboardRow, VocabularyRow } from '../services/AdminDataService';
import { FEATURES } from '../config/features';

export type Route='splash'|'home'|'tutorial'|'mode'|'worlds'|'world-detail'|'room'|'lobby'|'hunt3d'|'ar-check'|'ar-place'|'hunt-ar'|'result'|'review'|'progress'|'ranking'|'profile'|'settings'|'admin-dashboard'|'admin-create'|'admin-lobby'|'admin-live'|'admin-projector'|'admin-players'|'admin-classes'|'admin-history'|'admin-analytics'|'admin-vocab'|'admin-worlds'|'admin-accounts';
export interface HuntLike {targets:any[];score:{playerId:string;score:number;claims:number;wrong:number;combo:number;bestCombo:number};timeLeft():number;remaining():number;ended:boolean;}
export interface UISettings {quality:'auto'|'low'|'medium'|'high';sound:boolean;translation:boolean;debug:boolean;}
export interface UIContext {
  route:Route;hunt:HuntLike;arStatus?:{webxr:boolean;ar:boolean;hitTest:boolean;anchors:boolean};arPlaced?:boolean;
  cloudEnabled?:boolean;user?:AppUser|null;match?:MatchSnapshot|null;connectionStatus?:string;presence?:Record<string,any[]>;accounts?:AdminAccount[];accountsLoading?:boolean;
  selectedWorld?:string;pendingJoinCode?:string;localMode?:'practice'|'tutorial'|null;adminView?:'3d'|'top';battleMode?:'3d'|'ar';arSessionActive?:boolean;arCapability?:{checked:boolean;secure:boolean;webxr:boolean;immersiveAr:boolean;hitTest:boolean;anchors:boolean;domOverlay:boolean};classes?:LearningClass[];history?:MatchHistoryRow[];mastery?:any[];leaderboard?:LeaderboardRow[];arLeaderboard?:LeaderboardRow[];rankingMode?:'3d'|'ar';vocabularyRows?:VocabularyRow[];adminDataLoading?:boolean;settings?:UISettings;
}
const logo='/icons/icon-192.png';
const bi=(name:string)=>`<i class="bi bi-${name}"></i>`;
export function renderPage(c:UIContext){if(c.route==='admin-projector')return projector(c);return c.route.startsWith('admin-')?admin(c):mobile(c)}
function shell(title:string,body:string,nav=true){return `<main class="mobile-shell"><header class="mobile-head"><button class="icon-btn" data-action="back">${bi('chevron-left')}</button><div><b>${title}</b></div><img src="${logo}" class="mini-logo"></header>${body}${nav?bottomNav():''}</main>`}
function bottomNav(){return `<nav class="bottom-nav"><button data-nav="home">${bi('house-door')}<span>Home</span></button><button data-nav="worlds">${bi('globe2')}<span>Worlds</span></button><button data-nav="mode">${bi('controller')}<span>Battle</span></button><button data-nav="progress">${bi('graph-up-arrow')}<span>Progress</span></button><button data-nav="profile">${bi('person-circle')}<span>Profile</span></button></nav>`}
function cloudBadge(c:UIContext){const offline=(c.connectionStatus||'').includes('offline');return `<div class="cloud-badge ${c.cloudEnabled&&!offline?'on':'off'}">${bi(offline?'wifi-off':'broadcast-pin')} ${c.cloudEnabled?'Realtime Cloud':'Demo Lokal'}${c.connectionStatus?` · ${esc(c.connectionStatus)}`:''}</div>`}
function mobile(c:UIContext){const name=esc(c.user?.name||'Player');switch(c.route){
case 'splash':return `<main class="splash"><div class="splash-inner"><img src="${logo}" class="hero-logo"><h1>Arabic Hunt <em>Battle</em></h1><h2 dir="rtl">معركة البحث عن الكلمات العربية</h2><p>Masuk memakai akun yang dibuat admin.</p>${cloudBadge(c)}<form class="login-form" id="login-form"><label>Username<input id="login-username" autocomplete="username" placeholder="contoh: ahmad01" required></label><label>Password<div class="password-wrap"><input id="login-password" type="password" autocomplete="current-password" placeholder="Password" required><button type="button" class="password-toggle" data-action="toggle-password">${bi('eye')}</button></div></label><button class="primary" type="submit">${bi('box-arrow-in-right')} Masuk</button></form>${c.pendingJoinCode?`<div class="join-pending">Room <b>${esc(c.pendingJoinCode)}</b> akan dibuka setelah login.</div>`:''}<small>Admin-provisioned accounts • PWA • Realtime Multiplayer • v0.7.0</small></div></main>`;
case 'home':return `<main class="mobile-shell no-head"><section class="home-hero"><div class="avatar">${initial(name)}</div><div><small>Welcome,</small><h2>${name}</h2><div class="level">Arabic Hunt Battle · 3D + AR Classroom Edition</div></div><button data-go="settings" class="icon-btn">${bi('gear')}</button></section>${cloudBadge(c)}<section class="home-grid home-grid-v6"><button class="battle-card" data-go="mode">${bi('controller')}<b>Battle</b><span>Realtime 2–8 pemain</span></button><button class="green-card" data-action="start-practice">${bi('person-walking')}<b>Solo Practice</b><span>Latihan tanpa rebutan</span></button><button class="blue-card" data-go="tutorial">${bi('mortarboard')}<b>Tutorial</b><span>Belajar kontrol & target</span></button><button class="purple-card" data-go="progress">${bi('graph-up-arrow')}<b>Progress</b><span>Mastery & XP</span></button></section><section class="continue-card"><div><small>Playable Worlds</small><h3>Student Room + Kitchen</h3><b dir="rtl">غرفة الطالب · المطبخ</b><div class="progress"><i style="width:100%"></i></div></div><div class="room-emoji">🛏️🍳</div></section>${bottomNav()}</main>`;
case 'tutorial':return shell('Tutorial',`<section class="pad"><div class="tutorial-card">${bi('mouse2')}<h2>1. Putar kamera</h2><p>Drag layar untuk mengubah sudut pandang. Pinch/scroll untuk zoom.</p></div><div class="tutorial-card">${bi('search')}<h2>2. Baca target</h2><p>Panel kuning menunjukkan benda Arab yang harus dicari.</p></div><div class="tutorial-card">${bi('hand-index-thumb')}<h2>3. Tap benda</h2><p>Benar mendapat poin. Salah mengurangi 15 poin.</p></div><button class="primary" data-action="start-tutorial">${bi('play-fill')} Mulai Tutorial 3 Target</button></section>`);
case 'mode':return shell('Choose Mode',`<section class="pad"><h2>Pilih mode</h2><button class="mode-card three" data-action="select-mode" data-mode="3d"><div class="big-icon">${bi('controller')}</div><h2>3D Battle</h2><p>Realtime room, target, first claim, timer dan score.</p><b>Mulai →</b></button><button class="mode-card ar" data-action="select-mode" data-mode="ar"><div class="big-icon">${bi('phone')}</div><h2>AR Battle</h2><p>Miniature Student Room/Kitchen ditempatkan pada meja atau lantai melalui WebXR.</p><b>${bi('phone')} Cek perangkat →</b></button><button class="mode-card practice" data-action="start-practice"><div class="big-icon">${bi('person-walking')}</div><h2>Solo Practice</h2><p>Latihan dunia 3D tanpa pemain lain.</p><b>Latihan →</b></button></section>`);
case 'worlds':return shell('Choose a World',`<section class="pad world-grid-mobile">${WORLD_CARDS.map(w=>worldCard(w,c.selectedWorld)).join('')}</section>`);
case 'world-detail':return worldDetail(c);
case 'room':return roomPage(c);
case 'lobby':return lobbyPage(c);
case 'hunt3d':return huntPage(c,false);
case 'ar-check':{const a=c.arCapability||{checked:false,secure:false,webxr:false,immersiveAr:false,hitTest:false,anchors:false,domOverlay:false};return shell('AR Device Check',`<section class="pad ar-check"><div class="ar-device-hero">${bi('phone')}<h2>AR Battle Device Check</h2><p>AR memakai miniature virtual world yang sama untuk semua pemain — bukan mengenali benda rumah asli.</p></div>${check2('Secure HTTPS',a.secure,'secure')}${check2('WebXR API',a.webxr,'webxr')}${check2('Immersive AR Session',a.immersiveAr,'ar')}<div class="check-row optional"><span>i</span><b>Hit Test + Anchors</b><em>diverifikasi saat sesi AR dimulai</em></div><p class="hint" id="ar-check-note">${a.checked?(a.secure&&a.immersiveAr?'Perangkat siap. Hit Test akan diverifikasi ketika sesi AR dimulai.':'AR membutuhkan HTTPS dan perangkat/browser yang mendukung immersive-ar.'):'Memeriksa kemampuan perangkat…'}</p><button class="ghost" data-action="recheck-ar">${bi('arrow-clockwise')} Check Again</button><button class="primary" data-action="continue-ar-worlds" ${a.secure&&a.webxr&&a.immersiveAr?'':'disabled'}>${bi('globe2')} Choose AR World</button><div class="ar-safety"><b>${bi('exclamation-triangle')} Mainkan dengan aman</b><span>Pastikan area sekitar bebas hambatan. Jangan berjalan sambil terus menatap layar.</span></div></section>`,false)}
case 'ar-place':return shell('AR Setup',`<section class="pad"><div class="callout">AR setup sekarang dilakukan langsung di AR Battle agar sesi WebXR tidak terputus.</div><button class="primary" data-go="hunt-ar">${bi('phone')} Continue</button></section>`,false);
case 'hunt-ar':return huntPage(c,true);
case 'result':return resultPage(c);
case 'review':return shell('Review Vocabulary',`<section class="pad">${c.hunt.targets.map(t=>vocabRow(t.vocabId)).join('')}</section>`);
case 'progress':return progressPage(c);
case 'ranking':{const mode=c.rankingMode||'3d',rows=mode==='ar'?(c.arLeaderboard||[]):(c.leaderboard||[]);return shell('Leaderboard',`<section class="pad"><div class="tabs"><button class="${mode==='3d'?'active':''}" data-action="select-ranking-mode" data-mode="3d">${bi('controller')} 3D Ranking</button><button class="${mode==='ar'?'active':''}" data-action="select-ranking-mode" data-mode="ar">${bi('phone')} AR Ranking</button></div>${rows.length?`<div class="leaderboard-list">${rows.map((r,i)=>`<div class="rank-row"><b>${i<3?['🥇','🥈','🥉'][i]:i+1}</b><span><strong>${esc(r.displayName)}</strong><small>Level ${r.level} · ${r.matchesPlayed} ${mode.toUpperCase()} match · ${r.totalClaims} claims</small></span><em>${r.totalScore} pts</em></div>`).join('')}</div>`:`<div class="callout">Belum ada hasil ${mode.toUpperCase()} Battle. Selesaikan pertandingan resmi untuk mengisi leaderboard.</div>`}</section>`) };
case 'profile':return shell('My Profile',`<section class="pad profile"><div class="profile-top"><div class="avatar big">${initial(name)}</div><div><h2>${name}</h2><span>@${esc(c.user?.username||'player')} · ${esc(c.user?.role||'player')}</span></div></div>${cloudBadge(c)}<button class="menu-row" data-action="sign-out">${bi('box-arrow-right')} Sign out<span>›</span></button></section>`);
case 'settings':return settingsPage(c);
default:return ''}}
function worldCard(w:any,selected?:string){const locked=w.status!=='published';return `<button class="world-card ${locked?'locked':''} ${selected===w.id?'selected':''}" ${locked?`data-action="coming-soon" data-world="${w.id}"`:`data-action="select-world" data-world="${w.id}"`}><div class="world-art">${w.emoji}</div><div><h3 dir="rtl">${esc(w.arabic)}</h3><b>${esc(w.name)}</b><p>${esc(w.description)}</p><small>${locked?`${bi('lock-fill')} Coming Soon`:`${w.vocabularyCount} vocab · ${w.difficulty}`}</small></div></button>`}
function worldDetail(c:UIContext){const card=getWorldCard(c.selectedWorld),world=getWorld(card.id),samples=[...new Set(world.objects.filter(o=>o.vocabId).map(o=>o.vocabId!))].slice(0,8),ar=c.battleMode==='ar';return shell(card.name,`<section class="hero-room world-hero ${ar?'ar-world-hero':''}"><div>${card.emoji}</div>${ar?`<span class="ar-badge">${bi('phone')} AR MINIATURE</span>`:''}</section><section class="pad compact"><h1>${esc(card.name)}</h1><h2 dir="rtl">${esc(card.arabic)}</h2><p>${esc(card.description)}</p><section class="stats3"><div>${bi('translate')}<b>${card.vocabularyCount}</b><span>Vocabulary</span></div><div>${bi('box')}<b>${world.objects.length}</b><span>Objects</span></div><div>${bi(ar?'phone':'speedometer2')}<b>${ar?'AR':'Light'}</b><span>${ar?'Miniature':'3D Build'}</span></div></section><h3>Preview Vocabulary</h3><div class="vocab-preview">${samples.map(id=>{const v=VOCAB[id];return v?`<button data-speak="${id}"><b dir="rtl">${esc(v.arabic)}</b><span>${esc(v.idn)}</span>${bi('volume-up')}</button>`:''}).join('')}</div><button class="primary" data-go="room">${bi(ar?'phone':'controller')} ${ar?'AR Realtime Battle':'Realtime Battle'}</button>${!ar?`<button class="green" data-action="start-practice">${bi('person-walking')} Solo Practice</button>`:`<div class="callout ar-info">Arena AR berukuran tetap ±10% dari dunia 3D agar pertandingan antarpemain adil. Rotasi boleh disesuaikan dengan meja/lantai Anda.</div>`}</section>`)}
function roomPage(c:UIContext){const w=getWorldCard(c.selectedWorld),ar=c.battleMode==='ar';return shell(ar?'Create / Join AR Room':'Create / Join Room',`<section class="pad">${cloudBadge(c)}<div class="selected-world-pill">${w.emoji} <b>${esc(w.name)}</b><span class="mode-pill ${ar?'ar':''}">${bi(ar?'phone':'controller')} ${ar?'AR Battle':'3D Battle'}</span></div>${ar?`<div class="callout">Setelah masuk room, aktifkan kamera AR → scan permukaan → Place arena → Ready. Semua pemain AR bersaing hanya dengan pemain AR.</div>`:''}<div class="form-stack"><label>Players<select id="create-players"><option>2</option><option selected>4</option><option>6</option><option>8</option></select></label><label>Target Objects<select id="create-targets"><option>8</option><option selected>12</option><option>15</option><option>20</option></select></label><label>Time Limit<select id="create-duration"><option value="120">2 min</option><option value="180" selected>3 min</option><option value="300">5 min</option></select></label><label>Difficulty<select id="create-difficulty"><option>easy</option><option selected>medium</option><option>hard</option></select></label></div><button class="primary" data-action="create-room">${bi('plus-circle')} ${c.cloudEnabled?`Create ${ar?'AR ':' '}Realtime Room`:'Start Local Demo'}</button><div class="join-box"><label>Join code</label><input id="join-code" value="${esc(c.pendingJoinCode||'')}" placeholder="Contoh A7K92F" maxlength="8"><button class="ghost" data-action="join-room">${bi('door-open')} Join Realtime Room</button><small>Jika room yang dimasukkan adalah AR, aplikasi akan otomatis masuk ke AR setup.</small></div></section>`)}
function lobbyPage(c:UIContext){const s=c.match;if(!s)return shell('Lobby','<section class="pad"><p>Tidak ada room aktif.</p></section>');const me=s.players.find(p=>p.playerId===c.user?.id),players=s.players.filter(p=>p.role!=='spectator'),allReady=players.length>=2&&players.every(p=>p.ready),isHost=me?.role==='host',ar=s.match.gameMode==='ar';if(ar)return shell('AR Waiting Room',`<section class="pad"><div class="callout">Room ini memakai AR Battle. Lanjutkan ke AR setup untuk menempatkan arena sebelum Ready.</div><button class="primary" data-go="hunt-ar">${bi('phone')} Open AR Setup</button></section>`);return shell('Waiting Room',`<section class="pad">${cloudBadge(c)}<div class="big-code">${esc(s.match.roomCode)}</div><p class="center">${esc(getWorldCard(s.match.worldSlug).name)} · 3D · ${s.match.difficulty} · ${s.match.targetCount} target</p><div class="player-list">${players.map(p=>`<div><span>${p.connected?'🟢':'⚪'} ${esc(p.displayName)}</span><b>${p.ready?'READY':'WAIT'}</b></div>`).join('')}</div><button class="${me?.ready?'ghost':'green'}" data-action="toggle-ready">${me?.ready?`${bi('x-circle')} Not Ready`:`${bi('check-circle')} Ready`}</button>${isHost?`<button class="gold" data-action="start-match" ${allReady?'':'disabled'}>${bi('play-fill')} Start Battle</button>`:''}<p class="hint center">3D engine dipreload saat Anda Ready agar start lebih cepat.</p></section>`) }
function huntPage(c:UIContext,ar:boolean){const s=c.match,paused=s?.match.status==='paused',difficulty=s?.match.difficulty||'easy',waiting=s?.match.status==='waiting',me=s?.players.find(p=>p.playerId===c.user?.id),players=(s?.players||[]).filter(p=>p.role!=='spectator'),allReady=players.length>=2&&players.every(p=>p.ready),isHost=me?.role==='host';return `<main class="hunt-shell ${ar?'ar-hunt-shell':''}"><header class="hunt-top"><button data-action="leave-game">${bi('chevron-left')}</button><div><small>${esc(s?getWorldCard(s.match.worldSlug).name:getWorldCard(c.selectedWorld).name)} ${ar?'· AR':''}</small><b id="time-left">${fmt(c.hunt.timeLeft())}</b></div><div class="score-pill">${bi('star-fill')} <b id="score-value">${c.hunt.score.score}</b></div>${s&&c.user?.id===s.match.hostId&&c.user?.role!=='admin'&&!waiting?`<button class="host-pause" data-action="host-toggle-pause">${paused?bi('play-fill'):bi('pause-fill')}</button>`:''}</header><div class="hunt-command" id="hunt-command">${waiting?`<div class="command-label">${bi(ar?'phone':'hourglass-split')} ${ar?'SETUP AR ARENA':'WAITING ROOM'}</div><small>${ar?'Scan permukaan, Place arena, lalu Ready.':'Menunggu pertandingan dimulai.'}</small>`:commandHtml(c,difficulty)}</div><section class="game-stage ${ar?'ar-game-stage':''}"><canvas id="pc-canvas"></canvas><div class="scene-loading" id="scene-loading"><div class="spinner"></div><b id="scene-loading-label">Loading ${ar?'AR':'3D'} World…</b><div class="loading-track"><i id="scene-loading-bar" style="width:5%"></i></div><small>${ar?'Miniature world dimuat pada kualitas ringan untuk AR.':'Objek target dimuat lebih dulu agar permainan cepat siap.'}</small></div><div class="claim-feedback" id="claim-feedback"></div><div id="countdown-overlay" class="countdown-overlay"></div><div id="pause-overlay" class="pause-overlay" style="display:${paused?'flex':'none'}"><div>${bi('pause-circle-fill')}<h2>GAME PAUSED</h2><p>Menunggu admin melanjutkan pertandingan.</p></div></div><div id="connection-overlay" class="connection-overlay" style="display:${(c.connectionStatus||'').includes('offline')?'flex':'none'}">${bi('wifi-off')} Connection lost · reconnecting…</div><div id="perf-indicator" class="perf-indicator" style="display:${c.settings?.debug?'block':'none'}">FPS --</div>${ar?`<div id="ar-setup-overlay" class="ar-setup-overlay"><div class="ar-setup-card"><div class="ar-setup-head"><span class="ar-live-dot"></span><div><b>AR Battle Setup</b><small id="ar-anchor-state">Hit-test placement</small></div></div><div class="ar-steps"><div id="ar-step-session"><span>1</span><b>Start AR Camera</b></div><div id="ar-step-place"><span>2</span><b>Scan & Place Arena</b></div><div id="ar-step-ready"><span>3</span><b>Ready</b></div></div><p id="ar-message">Tekan Start AR untuk membuka kamera.</p><div class="ar-control-grid"><button class="primary" data-action="start-ar-session">${bi('camera')} Start AR</button><button class="ghost" data-action="reset-ar" data-ar-placement-control>${bi('arrow-clockwise')} Reset</button><button class="ghost" data-action="rotate-ar-left" data-ar-placement-control>${bi('arrow-counterclockwise')} Rotate</button><button class="ghost" data-action="rotate-ar-right" data-ar-placement-control>${bi('arrow-clockwise')} Rotate</button><button class="green" data-action="place-ar" data-ar-placement-control>⬡ Place Arena</button></div><div id="ar-waiting-text" class="ar-waiting-text">Place arena lalu tekan Ready.</div><div class="ar-ready-actions"><button class="green" data-action="toggle-ar-ready" ${c.arPlaced?'':'disabled'}>${me?.ready?`${bi('x-circle')} Not Ready`:`${bi('check-circle')} Ready`}</button>${isHost?`<button class="gold" data-action="start-ar-match" ${allReady?'':'disabled'}>${bi('play-fill')} Start AR Battle</button>`:''}</div><button class="ar-exit-session" data-action="end-ar-session">${bi('x-lg')} End AR Session</button></div></div>`:''}</section>${!waiting?`<section class="target-sheet"><div class="target-sheet-head"><b>Target List</b><span id="target-progress">${c.hunt.targets.length-c.hunt.remaining()}/${c.hunt.targets.length} selesai</span></div><div class="target-grid">${c.hunt.targets.map(t=>targetChip(t,c,difficulty)).join('')}</div></section><div class="score-strip">${scoreStrip(c)}</div>`:`<section class="target-sheet waiting-players"><div class="target-sheet-head"><b>${ar?'AR Players':'Players'}</b><span>${players.filter(p=>p.ready).length}/${players.length} ready</span></div><div class="player-list">${players.map(p=>`<div><span>${p.connected?'🟢':'⚪'} ${esc(p.displayName)}</span><b>${p.ready?'READY':'WAIT'}</b></div>`).join('')}</div></section>`}</main>`}
function commandHtml(c:UIContext,difficulty:string){const open=c.hunt.targets.filter(t=>!t.claimedBy);if(!open.length)return '<div class="command-done">✓ Semua target sudah ditemukan</div>';return `<div class="command-label">${bi('search')} CARI SALAH SATU TARGET</div><div class="command-targets">${open.slice(0,3).map((t:any,i:number)=>{const v=VOCAB[t.vocabId],mt=c.match?.targets.find(x=>x.objectId===t.objectId);return `<div class="command-target ${i===0?'focus':''}"><b dir="rtl">${esc(v?.arabic||mt?.arabic||t.vocabId)}</b>${difficulty==='easy'&&c.settings?.translation!==false?`<span>${esc(v?.idn||mt?.meaningId||'')}</span>`:''}${difficulty==='medium'&&v?`<button class="mini-speak" data-speak="${esc(t.vocabId)}">${bi('volume-up')}</button>`:''}</div>`}).join('')}</div><small>${open.length} target tersedia · target boleh dipilih bebas</small>`}
function targetChip(t:any,c:UIContext,difficulty:string){const v=VOCAB[t.vocabId],mt=c.match?.targets.find(x=>x.objectId===t.objectId);return `<div class="target-chip ${t.claimedBy?'claimed':''}" data-object="${esc(t.objectId)}"><i>${t.claimedBy?'✓':'○'}</i><span><b dir="rtl">${esc(v?.arabic||mt?.arabic||t.vocabId)}</b>${difficulty==='easy'&&c.settings?.translation!==false?`<small>${esc(v?.idn||mt?.meaningId||'')}</small>`:''}</span>${!t.claimedBy&&difficulty!=='hard'&&v?`<button data-speak="${esc(t.vocabId)}" class="chip-audio">${bi('volume-up')}</button>`:''}</div>`}
function scoreStrip(c:UIContext){if(!c.match)return `<span>${bi('star-fill')} Practice <b id="mini-score">${c.hunt.score.score}</b></span>`;return [...c.match.players].filter(p=>p.role!=='spectator').sort((a,b)=>b.score-a.score).slice(0,4).map((p,i)=>`<span>${i===0?'👑':i+1} ${esc(p.displayName)} <b>${p.score}</b></span>`).join('')}
function resultPage(c:UIContext){const p=c.match?.players.find(x=>x.playerId===c.user?.id);const score=p?.score??c.hunt.score.score,claims=p?.claims??c.hunt.score.claims,wrong=p?.wrongTaps??c.hunt.score.wrong,accuracy=claims+wrong?Math.round(claims/(claims+wrong)*100):100;return shell('Result',`<section class="pad result"><div class="result-medal">🏆</div><h1>${c.localMode?'Practice Complete':'Match Complete'}</h1><h2>${score} pts</h2><section class="stats3"><div>${bi('check-circle')}<b>${claims}</b><span>Correct</span></div><div>${bi('x-circle')}<b>${wrong}</b><span>Wrong</span></div><div>${bi('bullseye')}<b>${accuracy}%</b><span>Accuracy</span></div></section><button class="primary" data-go="review">${bi('journal-text')} Review Words</button>${c.match&&c.user?.id===c.match.match.hostId?`<button class="green" data-action="rematch">${bi('arrow-repeat')} Rematch Same Settings</button>`:''}<button class="ghost" data-go="home">Home</button></section>`)}
function progressPage(c:UIContext){const rows=(c.mastery||[]).filter((x:any)=>x.user_id===c.user?.id);return shell('My Progress',`<section class="pad"><h3>World Mastery</h3>${rows.length?rows.map((x:any)=>mastery(x.worlds?.name||'World',Math.round(Number(x.mastery||0)))).join(''):`${mastery('Student Room',0)}${mastery('Kitchen',0)}`}<div class="callout">Mastery diperbarui saat match resmi selesai. Practice tidak memengaruhi ranking.</div></section>`)}
function settingsPage(c:UIContext){const s=c.settings||{quality:'auto',sound:true,translation:true,debug:false};return shell('Settings',`<section class="pad form-stack"><label>3D Quality<select id="setting-quality"><option value="auto" ${s.quality==='auto'?'selected':''}>Auto</option><option value="low" ${s.quality==='low'?'selected':''}>Low</option><option value="medium" ${s.quality==='medium'?'selected':''}>Medium</option><option value="high" ${s.quality==='high'?'selected':''}>High</option></select></label><label class="toggle-row">Sound Effects<input id="setting-sound" type="checkbox" ${s.sound?'checked':''}></label><label class="toggle-row">Show Translation<input id="setting-translation" type="checkbox" ${s.translation?'checked':''}></label><label class="toggle-row">Performance Stats<input id="setting-debug" type="checkbox" ${s.debug?'checked':''}></label><button class="primary" data-action="save-settings">${bi('save')} Save Settings</button></section>`)}

function admin(c:UIContext){const r=c.route;const content=r==='admin-dashboard'?adminDashboard(c):r==='admin-create'?adminCreate(c):r==='admin-lobby'?adminLobby(c):r==='admin-live'?adminLive(c):r==='admin-players'?adminPlayers(c):r==='admin-classes'?adminClasses(c):r==='admin-history'?adminHistory(c):r==='admin-accounts'?adminAccounts(c):r==='admin-analytics'?adminAnalytics(c):r==='admin-vocab'?adminVocab(c):adminWorlds(c);const nav:[Route,string,string][]=[['admin-dashboard','speedometer2','Dashboard'],['admin-create','controller','Matches'],['admin-worlds','globe2','Worlds'],['admin-vocab','translate','Vocabulary'],['admin-players','people','Players'],['admin-classes','mortarboard','Classes'],['admin-accounts','person-gear','User Accounts'],['admin-history','clock-history','Match History'],['admin-analytics','bar-chart-line','Analytics']];return `<main class="admin-layout"><aside class="sidebar"><div class="brand"><img src="${logo}"><div><b>Arabic Hunt Battle</b><small>Admin v0.7.0</small></div></div><nav class="admin-nav">${nav.map(([id,icon,n])=>`<button data-go="${id}" class="${r===id?'active':''}">${bi(icon)}<span>${n}</span></button>`).join('')}</nav><div class="admin-user">${bi('person-circle')} ${esc(c.user?.name||'Admin')}<br><small>@${esc(c.user?.username||'admin')} · ${esc(c.connectionStatus||'idle')}</small><button class="sidebar-signout" data-action="sign-out">${bi('box-arrow-right')} Sign out</button></div></aside><section class="admin-main">${content}</section></main>`}
function adminDashboard(c:UIContext){const s=c.match;return `<div class="admin-head"><div><h1>${bi('speedometer2')} Dashboard</h1><p>Realtime classroom control center.</p></div><button class="primary small" data-go="admin-create">${bi('plus-circle')} Create Match</button></div>${cloudBadge(c)}<section class="kpis"><div><b>${s?.players.filter(p=>p.role!=='spectator').length||0}</b><span>Current Players</span></div><div><b>${s?.match.status||'—'}</b><span>Match Status</span></div><div><b>${s?.targets.filter(t=>!!t.claimedBy).length||0}</b><span>Claims</span></div><div><b>${c.connectionStatus||'idle'}</b><span>Realtime</span></div></section><div class="admin-grid"><section class="panel"><h3>Current Match</h3>${s?`<button class="match-row" data-go="admin-live">${esc(s.match.roomCode)} · ${esc(getWorldCard(s.match.worldSlug).name)} · ${s.players.length}/${s.match.maxPlayers}<span>${s.match.status}</span></button>`:'<p>Belum ada match aktif pada browser ini.</p>'}</section><section class="panel"><h3>Quick Actions</h3><button class="primary" data-go="admin-create">${bi('controller')} Create Match</button><button class="green" data-go="admin-worlds">${bi('globe2')} Manage Worlds</button><button class="ghost" data-go="admin-history">${bi('clock-history')} Match History</button></section></div>`}
function adminCreate(c:UIContext){return `<div class="admin-head"><div><h1>${bi('plus-circle')} Create New Match</h1><p>Pilih world, kategori materi, kelas, target dan durasi.</p></div></div>${cloudBadge(c)}<section class="panel form-grid"><label>Game Mode<select id="admin-mode"><option value="3d">3D Battle</option><option value="ar">AR Battle</option></select></label><label>World<select id="admin-world"><option value="student-room">Student Room</option><option value="kitchen">Kitchen</option></select></label><label>Class / Group<select id="admin-class"><option value="">Open Room</option>${(c.classes||[]).map(x=>`<option value="${x.id}">${esc(x.name)} (${x.memberCount})</option>`).join('')}</select></label><label>Players<select id="admin-players"><option>2</option><option selected>4</option><option>6</option><option>8</option></select></label><label>Targets<select id="admin-targets"><option>8</option><option selected>12</option><option>15</option><option>20</option></select></label><label>Match Time<select id="admin-duration"><option value="120">2 Minutes</option><option value="180" selected>3 Minutes</option><option value="300">5 Minutes</option></select></label><label>Difficulty<select id="admin-difficulty"><option>easy</option><option selected>medium</option><option>hard</option></select></label><fieldset class="category-field span2"><legend>Target Categories</legend>${['furniture','study','room','object','appliance','utensil','cookware','food','spatial'].map(x=>`<label><input type="checkbox" name="admin-category" value="${x}"> ${x}</label>`).join('')}<small>Kosong = semua kategori yang sesuai difficulty.</small></fieldset><button class="primary span2" data-action="admin-create-room">${bi('controller')} Create Realtime Match</button></section>`}
function adminLobby(c:UIContext){const s=c.match;if(!s)return `<div class="admin-head"><h1>No Match</h1></div>`;const participants=s.players.filter(p=>p.role!=='spectator'),spectators=s.players.filter(p=>p.role==='spectator'),allReady=participants.length>=2&&participants.every(p=>p.ready);return `<div class="admin-head"><div><h1>${bi('people')} Waiting Room</h1><p>${esc(getWorldCard(s.match.worldSlug).name)} · ${s.match.gameMode.toUpperCase()} Battle · ${s.match.targetCount} target · ${s.match.durationSeconds/60} min</p></div></div><div class="admin-grid"><section class="panel qr-panel"><div class="big-code">${esc(s.match.roomCode)}</div><img id="room-qr" class="room-qr" alt="QR room"><p>Scan QR atau masukkan kode room.</p><button class="ghost compact-btn" data-action="copy-room-code">${bi('copy')} Copy Room Code</button></section><section class="panel"><h3>Participants</h3>${participants.map(p=>`<div class="admin-player">${p.connected?'🟢':'⚪'} ${esc(p.displayName)} · <b>${p.ready?'Ready':'Not Ready'}</b></div>`).join('')||'<p>Waiting for players...</p>'}${spectators.length?`<h3>Admin / Spectator</h3>${spectators.map(p=>`<div class="admin-player">${bi('eye')} ${esc(p.displayName)}</div>`).join('')}`:''}<button class="gold" data-action="start-match" ${allReady?'':'disabled'}>${bi('play-fill')} Start Battle</button></section></div>`}
function adminLive(c:UIContext){const s=c.match,players=[...(s?.players||[])].filter(p=>p.role!=='spectator').sort((a,b)=>b.score-a.score),paused=s?.match.status==='paused';return `<div class="admin-head"><div><h1>${bi('broadcast')} Live Match</h1><p>${s?`${esc(getWorldCard(s.match.worldSlug).name)} · ${s.match.gameMode.toUpperCase()} Battle · ${esc(s.match.roomCode)} · ${s.match.status}`:'No active match'}</p></div><div class="timer-box" id="admin-time">${fmt(c.hunt.timeLeft())}</div></div><div class="spectator-toolbar"><button class="${c.adminView==='3d'?'active':''}" data-action="admin-view" data-view="3d">${bi('box')} 3D Spectator</button><button class="${c.adminView==='top'?'active':''}" data-action="admin-view" data-view="top">${bi('map')} Top View</button><button data-action="spectator-overview">${bi('arrows-fullscreen')} Overview</button><button data-action="open-projector">${bi('projector')} Projector</button></div><div class="live-layout"><section class="panel map-panel">${c.adminView==='3d'?`<div class="spectator-3d"><canvas id="admin-pc-canvas"></canvas><div id="admin-3d-labels" class="spectator-labels"></div><div id="admin-scene-loading" class="admin-scene-loading">Loading spectator 3D…</div></div>`:`<div class="topdown"><div class="bed-block">ZONE A</div><div class="desk-block">ZONE B</div><div class="shelf-block">ZONE C</div><div id="admin-pose-layer"></div></div>`}<div class="live-controls">${s?.match.status==='ended'?`<button class="green" data-action="rematch">${bi('arrow-repeat')} Rematch Same Settings</button><button data-go="admin-history">${bi('clock-history')} Match History</button>`:paused?`<button class="green" data-action="admin-resume">${bi('play-fill')} Resume</button><button class="danger" data-action="admin-end">${bi('stop-fill')} End Match</button>`:`<button data-action="admin-pause">${bi('pause-fill')} Pause</button><button class="danger" data-action="admin-end">${bi('stop-fill')} End Match</button>`}</div></section><section class="panel live-side"><h3>Live Score</h3><div id="admin-live-scores">${players.map((p,i)=>scoreRow(p,i)).join('')||'<p>Waiting for players...</p>'}</div><h3>Live Activity</h3><div class="feed" id="admin-live-feed">${activityHtml(s)}</div><h3>Follow Player</h3><div class="follow-grid">${players.map(p=>`<button data-action="follow-player" data-player="${p.playerId}">${bi('crosshair')} ${esc(p.displayName)}</button>`).join('')}</div></section></div>`}
function adminPlayers(c:UIContext){const s=c.match;return `<div class="admin-head"><div><h1>${bi('people')} Player Monitor</h1><p>Presence, score, wrong taps, claims dan connection.</p></div></div>${table(['Player','Status','Role','Ready','Score','Claims','Wrong'],(s?.players||[]).map(p=>[p.displayName,p.connected?'Online':'Offline',p.role,p.ready?'Ready':'No',String(p.score),String(p.claims),String(p.wrongTaps)]))}`}
function adminClasses(c:UIContext){return `<div class="admin-head"><div><h1>${bi('mortarboard')} Classes</h1><p>Kelompokkan akun siswa agar room dapat dibatasi per kelas.</p></div></div><div class="admin-grid"><section class="panel"><h3>Create Class</h3><div class="form-stack"><input id="class-name" placeholder="Contoh: Kelas 7A"><button class="primary" data-action="create-class">${bi('plus-circle')} Create Class</button></div><h3>Classes</h3>${(c.classes||[]).map(x=>`<div class="class-row"><b>${esc(x.name)}</b><span>${x.memberCount} members</span></div>`).join('')||'<p>Belum ada kelas.</p>'}</section><section class="panel"><h3>Assign Student</h3><div class="form-stack"><label>Class<select id="assign-class">${(c.classes||[]).map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')}</select></label><label>User<select id="assign-user">${(c.accounts||[]).filter(a=>a.role==='player').map(a=>`<option value="${a.id}">${esc(a.displayName)} (@${esc(a.username)})</option>`).join('')}</select></label><button class="green" data-action="assign-class">${bi('person-plus')} Assign</button></div></section></div>`}
function adminHistory(c:UIContext){return `<div class="admin-head"><div><h1>${bi('clock-history')} Match History</h1><p>30 match terakhir.</p></div></div>${table(['Room','World','Mode','Players','Targets','Status','Date'],(c.history||[]).map(x=>[x.roomCode,x.worldName,x.gameMode.toUpperCase(),String(x.playerCount),String(x.targetCount),x.status,new Date(x.createdAt).toLocaleString('id-ID')]))}`}
function adminAccounts(c:UIContext){const accounts=c.accounts||[];return `<div class="admin-head"><div><h1>${bi('person-gear')} User Accounts</h1><p>Admin membuat username/password untuk guru dan siswa.</p></div></div>${cloudBadge(c)}<div class="admin-grid account-grid"><section class="panel"><h3>${bi('person-plus')} Buat Akun</h3><div class="form-stack admin-account-form"><label>Username<input id="account-username" placeholder="ahmad01" maxlength="32"></label><label>Nama Tampilan<input id="account-display-name" placeholder="Ahmad"></label><label>Password<div class="password-wrap light"><input id="account-password" type="password" placeholder="Minimal 8 karakter"><button type="button" class="password-toggle" data-action="toggle-account-password">${bi('eye')}</button></div></label><label>Role<select id="account-role"><option value="player">Player</option><option value="admin">Admin</option></select></label><button class="primary" data-action="admin-create-user">Buat Akun</button></div></section><section class="panel account-list"><div class="account-list-head"><h3>Daftar Akun</h3><button class="ghost smallish" data-action="refresh-accounts">${bi('arrow-clockwise')} Refresh</button></div>${c.accountsLoading?'<p>Memuat akun...</p>':accounts.length?accounts.map(a=>`<div class="account-row"><div class="account-avatar">${initial(a.displayName)}</div><div class="account-meta"><b>${esc(a.displayName)}</b><span>@${esc(a.username)} · ${esc(a.role)}</span><small>Level ${a.level} · ${a.xp} XP · ${a.active?'Aktif':'Nonaktif'}</small></div><div class="account-actions"><button data-action="reset-user-password" data-user-id="${a.id}" data-user-name="${esc(a.username)}">Reset Password</button><button class="${a.active?'danger-soft':'green-soft'}" data-action="toggle-user-active" data-user-id="${a.id}" data-active="${a.active?'1':'0'}">${a.active?'Nonaktifkan':'Aktifkan'}</button></div></div>`).join(''):'<p>Belum ada akun.</p>'}</section></div>`}
function adminAnalytics(c:UIContext){const rows=c.mastery||[];return `<div class="admin-head"><div><h1>${bi('bar-chart-line')} Learning Analytics</h1><p>Mastery dan best accuracy dari pertandingan resmi.</p></div></div><section class="kpis"><div><b>${rows.length}</b><span>Progress Records</span></div><div><b>${Math.round(rows.reduce((a:number,x:any)=>a+Number(x.mastery||0),0)/Math.max(1,rows.length))}%</b><span>Avg Mastery</span></div><div><b>${Math.round(rows.reduce((a:number,x:any)=>a+Number(x.best_accuracy||0),0)/Math.max(1,rows.length))}%</b><span>Avg Best Accuracy</span></div><div><b>2</b><span>Playable Worlds</span></div></section>${table(['Player','World','Mastery','Best Accuracy','Updated'],rows.slice(0,30).map((x:any)=>[x.profiles?.display_name||'Player',x.worlds?.name||'World',`${Math.round(Number(x.mastery||0))}%`,`${Math.round(Number(x.best_accuracy||0))}%`,new Date(x.updated_at).toLocaleString('id-ID')]))}`}
function adminVocab(c:UIContext){const rows=c.vocabularyRows||[];return `<div class="admin-head"><div><h1>${bi('translate')} Vocabulary Manager</h1><p>Tambah/edit vocabulary global lalu mapping ke object world.</p></div></div><div class="admin-grid"><section class="panel"><h3>Vocabulary Editor</h3><div class="form-stack"><input id="vocab-slug" placeholder="slug: spoon"><input id="vocab-arabic" placeholder="Arabic"><input id="vocab-translit" placeholder="Transliteration"><input id="vocab-idn" placeholder="Bahasa Indonesia"><input id="vocab-en" placeholder="English"><input id="vocab-category" placeholder="Category"><select id="vocab-difficulty"><option>easy</option><option>medium</option><option>hard</option></select><input id="vocab-points" type="number" value="50"><button class="primary" data-action="save-vocab">${bi('save')} Save Vocabulary</button></div></section><section class="panel"><h3>Current Vocabulary · ${rows.length}</h3>${tableInner(['Slug','Arabic','Meaning','Category','Difficulty','Points'],(rows.length?rows.map(v=>[v.slug,v.arabic,v.meaningId||'',v.category||'',v.difficulty||'',String(v.points)]):Object.values(VOCAB).map(v=>[v.id,v.arabic,v.idn,v.category,v.difficulty,String(v.points)])).slice(0,80))}</section></div>`}
function adminWorlds(c:UIContext){const w=getWorld(c.selectedWorld);return `<div class="admin-head"><div><h1>${bi('globe2')} World Manager</h1><p>Student Room dan Kitchen playable; kartu lain sudah disiapkan sebagai Coming Soon.</p></div></div><div class="world-admin-grid">${WORLD_CARDS.map(x=>`<div class="world-admin ${x.status!=='published'?'disabled':''}"><div>${x.emoji}</div><h3>${esc(x.name)}</h3><p>${x.status==='published'?'Published · Playable':'Coming Soon'}</p>${x.status==='published'?`<button data-action="admin-select-world" data-world="${x.id}">${bi('pencil-square')} Configure</button>`:''}</div>`).join('')}</div><section class="panel world-mapper"><h3>Object Mapping · ${esc(getWorldCard(c.selectedWorld).name)}</h3><div class="form-grid"><label>Object<select id="map-object">${w.objects.filter(o=>o.interactive).map(o=>`<option value="${o.id}">${esc(o.name)} · ${o.id}</option>`).join('')}</select></label><label>Vocabulary Slug<input id="map-vocab" list="vocab-slugs" placeholder="contoh: spoon"><datalist id="vocab-slugs">${(c.vocabularyRows?.length?c.vocabularyRows.map(v=>({id:v.slug,arabic:v.arabic,idn:v.meaningId||''})):Object.values(VOCAB)).map((v:any)=>`<option value="${esc(v.id)}">${esc(v.arabic)} · ${esc(v.idn)}</option>`).join('')}</datalist></label><button class="green span2" data-action="map-world-object">${bi('link-45deg')} Save Mapping</button></div></section>`}
function projector(c:UIContext){const s=c.match,players=[...(s?.players||[])].filter(p=>p.role!=='spectator').sort((a,b)=>b.score-a.score);return `<main class="projector"><header><img src="${logo}"><div><h1>ARABIC HUNT BATTLE</h1><p>${s?`${esc(getWorldCard(s.match.worldSlug).name)} · ${s.match.gameMode.toUpperCase()} Battle · Room ${esc(s.match.roomCode)}`:'No active match'}</p></div><div id="admin-time" class="projector-time">${fmt(c.hunt.timeLeft())}</div></header><section class="projector-grid"><div class="projector-board"><h2>LIVE SCORE</h2><div id="admin-live-scores">${players.map((p,i)=>`<div class="projector-score"><b>${i+1}</b><span>${esc(p.displayName)}</span><strong>${p.score}</strong></div>`).join('')}</div></div><div class="projector-side"><img id="room-qr" class="projector-qr"><div class="projector-code">${esc(s?.match.roomCode||'------')}</div><h2>LIVE ACTIVITY</h2><div id="admin-live-feed" class="projector-feed">${activityHtml(s)}</div></div></section></main>`}
function activityHtml(s?:MatchSnapshot|null){if(!s?.activity?.length)return 'No activity yet';return s.activity.slice(0,10).map(a=>`<div class="feed-row ${a.correct?'good':'bad'}">${a.correct?bi('check-circle-fill'):bi('x-circle-fill')} <b>${esc(a.displayName)}</b> ${a.correct?`found <span dir="rtl">${esc(a.arabic||a.objectId)}</span>`:`wrong tap · ${esc(a.objectId)}`} <strong>${a.points>0?'+':''}${a.points}</strong></div>`).join('')}
function scoreRow(p:any,i:number){return `<div class="score-row"><span>${i===0?'👑':i+1} ${esc(p.displayName)} <small>${p.connected?'● online':'○ offline'}</small></span><b>${p.score}</b></div>`}
function check(label:string,ok:boolean){return `<div class="check-row ${ok?'ok':'bad'}"><span>${ok?'✓':'×'}</span><b>${label}</b><em>${ok?'Ready':'Unavailable'}</em></div>`}
function check2(label:string,ok:boolean,key:string){return `<div class="check-row ${ok?'ok':'bad'}" data-ar-check="${key}"><span>${ok?'✓':'×'}</span><b>${label}</b><em>${ok?'Ready':'Unavailable'}</em></div>`}
function vocabRow(id:string){const v=VOCAB[id];if(!v)return '';return `<div class="vocab-row"><button data-speak="${id}">${bi('volume-up')}</button><div><b dir="rtl">${esc(v.arabic)}</b><small>${esc(v.latin)}</small></div><span>${esc(v.idn)}</span></div>`}
function mastery(name:string,p:number){return `<div class="mastery"><span>${esc(name)}</span><div class="progress"><i style="width:${p}%"></i></div><b>${p}%</b></div>`}
function table(headers:string[],rows:string[][]){return `<section class="panel table-wrap">${tableInner(headers,rows)}</section>`}
function tableInner(headers:string[],rows:string[][]){return `<div class="table-wrap"><table><thead><tr>${headers.map(x=>`<th>${esc(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${esc(x)}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`}
function fmt(ms:number){const m=Math.max(0,Math.floor(ms/60000)),s=Math.max(0,Math.floor((ms%60000)/1000));return `${m}:${String(s).padStart(2,'0')}`}
function esc(x:any){return String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]!))}
function initial(s:string){return (s.trim()[0]||'P').toUpperCase()}
